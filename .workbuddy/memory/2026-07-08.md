# 2026-07-08 工作日志

## 笔记模块全方位 Code Review
- 审查范围: pages/notes/ (index/add/detail)、note-manager.js、cloud.js 笔记同步段、record/interaction 云函数、child-storage.js、auth.js、util.js
- 产出报告: D:/project/geren/miniprogram/NOTES_MODULE_REVIEW.md
- 发现问题: 3 个 P0 + 6 个 P1 + 6 个 P2 + 5 个 P3
- 关键发现:
  - P0-1: detail.js 详情页 loadNote 只读本地不校验 visibility，private 笔记可越权查看
  - P0-2: 删除/编辑笔记时云存储图片文件不清理，孤儿文件泄漏
  - P0-3: index.js loadNotes 本地展示未按 visibility 过滤，笔记改 private 后本地缓存残留仍展示
  - P1-1: notes 误入 child-storage 的 CHILD_KEYS(本地按孩子隔离)，与"笔记家庭级"设计冲突，应移除
  - P1-4: record/index.js updateRecord 调 canEdit(member, record) 未传 collection/field，字段级权限失效
  - P1-5: 仓库无数据库/存储安全规则文件，无法审计
- 权限模型: visibility 三级(family/designated/private) + familyId 隔离，写入和列表环节校验完整，详情读取环节断裂
- 图片同步: 上传有 takePhoto+uploadNote 双兜底较健壮，但清理环节完全缺失; 两套云路径策略并存

## 设计澄清与报告更新
- 用户确认: 笔记是家庭级数据，有意不按 childId 隔离，靠 visibility 控制权限。云端 listRecords 不按 childId 过滤是设计正确
- 重新定性 P1-1: 不是"本地/云端设计冲突"，而是"本地存储实现 bug"——notes 误入 CHILD_KEYS，应移除
- 新增 P0-3: index.js loadNotes 本地展示未按 visibility 过滤
- 确认 UI 层(add.wxml)权限选择完整实现(family/designated/private 三选 + 指定成员多选)
- 报告已更新: P0 从 2 项增至 3 项，总结段重写为"设计已实现、本地层 3 处缺陷"

## 修复实施（karpathy-guidelines）
- 已修复 13 项: P0-1/P0-2/P0-3/P1-1/P1-2/P1-3/P1-4/P1-6/P2-1/P2-2/P2-4/P2-5/P2-6
- 已补配置: P1-5 (cloudfunctions/cloudbase/database.rules.json)
- 评估不改 5 项: P2-3(历史数据兼容,需运维迁移)/P3-1(语义驱动非bug)/P3-3(死代码surgical不删)/P3-4(原生支持)/P3-5(云端幂等兜底)
- 改动文件: child-storage.js/note-manager.js/util.js/cloud.js/detail.js/detail.wxml/index.js/add.js/record/index.js/interaction/index.js + 新建 database.rules.json
- 验证: node --check 全部 9 个 JS 文件语法通过
- 关键修复点:
  - record/index.js 新增 deleteCloudFiles 辅助函数,removeRecord 删记录前清云文件,updateRecord 编辑图片清差集
  - detail.js/index.js 加 visibility 本地校验(创建者/admin/family/designated 含本人/private 仅创建者)
  - cloud.js uploadNote 统一随机云路径+跳过已持久化路径; fetchNotes 改分页循环(上限5000)
  - util.js saveImageToPersistent 加 prefix 参数,笔记用 'note' 前缀不触发 drawings LRU
  - add.js 新建复用 noteManager.addNote(消除重复), takePhoto 失败提示

## Code Review 后续修复
- reviewer 发现 6 个问题(1 Critical + 5 Important)，已全部修复:
  - C-1: 修复误删封面图 - 删除 updateRecord 中 imagePath 独立清理块,仅保留 images 差集
  - I-1: 离线笔记迁移 - cloud.js 新增 migrateNotesStorage(),fetchNotes 首次执行时合并旧 notes_<childId> 到全局 key
  - I-2: updateRecord 字段校验 - 在 notes 校验块加 title/content/images/tags 长度校验
  - I-3: 字段级权限 - updateRecord 加 fieldPerms 遍历,逐字段调 canEdit
  - I-4: 删除顺序优化 - removeRecord 改为先删 DB 再清云文件(避免幽灵记录)
  - I-5: 详情页刷新 - detail.js onShow 增加 cloud.fetchNotes() 同步最新笔记
  - M-2: addRecord 非字符串拒绝 - 校验 title 必须为 string
