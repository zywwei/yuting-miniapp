/* 刷牙打卡页样式 */
.brushing-container {
  min-height: 100vh;
  background: linear-gradient(180deg, #FFF0F6 0%, #FFF5F8 50%, #FFF8F5 100%);
  padding-bottom: env(safe-area-inset-bottom);
}

/* 状态栏占位 */
.status-bar {
  width: 100%;
  background: linear-gradient(135deg, #FF9AAB, #FF6B8A);
}

/* 顶部工具栏 */
.toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 20rpx;
  background: linear-gradient(135deg, #FF9AAB, #FF6B8A);
}

.tool-left, .tool-right {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.tool-btn {
  padding: 12rpx 16rpx;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.2);
  color: white;
  font-size: 24rpx;
}

.tool-btn:active {
  background: rgba(255, 255, 255, 0.4);
}

.tool-center {
  flex: 1;
  text-align: center;
}

.tool-title {
  color: white;
  font-size: 30rpx;
  font-weight: bold;
}

/* 顶部信息区 */
.header-info {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30rpx 40rpx;
}

.date-display {
  display: flex;
  flex-direction: column;
}

.date-day {
  font-size: 48rpx;
  font-weight: bold;
  color: #333;
}

.date-week {
  font-size: 28rpx;
  color: #999;
  margin-top: 4rpx;
}

.streak-badge {
  display: flex;
  align-items: center;
  background: white;
  padding: 16rpx 28rpx;
  border-radius: 50rpx;
  box-shadow: 0 4rpx 16rpx rgba(255, 154, 171, 0.2);
}

.streak-icon {
  font-size: 36rpx;
  margin-right: 8rpx;
}

.streak-text {
  font-size: 26rpx;
  color: #FF6B8A;
  font-weight: bold;
}

/* 打卡卡片 */
.cards-area {
  padding: 0 30rpx;
}

.brushing-card {
  background: white;
  border-radius: 32rpx;
  padding: 40rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 6rpx 24rpx rgba(255, 154, 171, 0.15);
  position: relative;
  overflow: hidden;
}

.brushing-card.done {
  background: linear-gradient(135deg, #FFF8F0, #FFF0F3);
  border: 2rpx solid #FFD6DE;
}

.brushing-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 8rpx;
  height: 100%;
  background: linear-gradient(180deg, #FF9AAB, #FF6B8A);
}

.brushing-card.done::before {
  background: linear-gradient(180deg, #90EE90, #4CAF50);
}

.card-icon {
  font-size: 64rpx;
  margin-bottom: 12rpx;
}

.card-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 8rpx;
}

.card-desc {
  font-size: 26rpx;
  color: #999;
  margin-bottom: 24rpx;
}

.card-action {
  margin-top: 10rpx;
  display: flex;
  gap: 20rpx;
}

.check-btn {
  display: inline-block;
  background: linear-gradient(135deg, #FF9AAB, #FF6B8A);
  color: white;
  padding: 24rpx 60rpx;
  border-radius: 50rpx;
  font-size: 30rpx;
  font-weight: bold;
  box-shadow: 0 6rpx 20rpx rgba(255, 107, 138, 0.25);
  transition: all 0.2s;
}

.check-btn:active {
  transform: scale(0.96);
}

.check-btn.timer-btn {
  background: linear-gradient(135deg, #81C784, #4CAF50);
  box-shadow: 0 6rpx 20rpx rgba(76, 175, 80, 0.25);
}

.check-btn.direct-btn {
  background: linear-gradient(135deg, #4FC3F7, #0288D1);
  box-shadow: 0 6rpx 20rpx rgba(2, 136, 209, 0.25);
}

/* 已完成记录展示 */
.record-display {
  margin-top: 10rpx;
}

/* 多图横向滚动 */
.photo-scroll {
  white-space: nowrap;
  margin-bottom: 16rpx;
}

.photo-list {
  display: inline-flex;
  gap: 16rpx;
}

.photo-item {
  position: relative;
  flex-shrink: 0;
}

.record-image {
  width: 200rpx;
  height: 200rpx;
  border-radius: 20rpx;
  background: #F8F8F8;
}

.photo-single {
  position: relative;
  display: inline-block;
}

.photo-delete {
  position: absolute;
  top: -12rpx;
  right: -12rpx;
  width: 40rpx;
  height: 40rpx;
  background: #FF4444;
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22rpx;
  z-index: 10;
}

.photo-edit {
  position: absolute;
  bottom: 8rpx;
  right: 8rpx;
  width: 44rpx;
  height: 44rpx;
  background: rgba(0, 0, 0, 0.5);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22rpx;
  z-index: 10;
}

.photo-add {
  width: 200rpx;
  height: 200rpx;
  border-radius: 20rpx;
  background: #F5F5F5;
  border: 3rpx dashed #DDD;
  display: flex;
  align-items: center;
  justify-content: center;
}

.photo-add-icon {
  font-size: 60rpx;
  color: #CCC;
}

/* 刷牙详情标签 */
.record-detail {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 16rpx;
}

.detail-tag {
  font-size: 22rpx;
  color: #666;
  background: #F5F5F5;
  padding: 6rpx 16rpx;
  border-radius: 16rpx;
}

.record-image-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 64rpx;
  background: linear-gradient(135deg, #E8F5E9, #F1F8E9);
}

.record-score {
  display: flex;
  gap: 8rpx;
  margin-top: 16rpx;
}

.score-star {
  font-size: 28rpx;
  opacity: 0.3;
}

.score-star.active {
  opacity: 1;
}

.record-note {
  font-size: 24rpx;
  color: #999;
  margin-top: 12rpx;
  background: rgba(255, 154, 171, 0.1);
  padding: 10rpx 20rpx;
  border-radius: 12rpx;
  display: inline-block;
}

.add-photo-btn {
  display: inline-block;
  background: linear-gradient(135deg, #4FC3F7, #0288D1);
  color: white;
  padding: 16rpx 36rpx;
  border-radius: 40rpx;
  font-size: 26rpx;
  font-weight: bold;
  box-shadow: 0 4rpx 16rpx rgba(2, 136, 209, 0.25);
}

.add-photo-btn:active {
  transform: scale(0.96);
}

.record-extra-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 16rpx;
}

.retry-btn {
  display: inline-block;
  background: linear-gradient(135deg, #81C784, #4CAF50);
  color: white;
  padding: 16rpx 36rpx;
  border-radius: 40rpx;
  font-size: 26rpx;
  font-weight: bold;
  box-shadow: 0 4rpx 16rpx rgba(76, 175, 80, 0.25);
}

.retry-btn:active {
  transform: scale(0.96);
}

/* 鼓励语 */
.encourage-area {
  text-align: center;
  padding: 30rpx 60rpx;
}

.encourage-text {
  font-size: 28rpx;
  color: #FF6B8A;
  background: rgba(255, 255, 255, 0.7);
  padding: 20rpx 40rpx;
  border-radius: 40rpx;
  display: inline-block;
}

/* 弹窗 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
}

.modal-content {
  width: 80%;
  background: white;
  border-radius: 32rpx;
  padding: 50rpx 40rpx;
  text-align: center;
}

.modal-title {
  font-size: 40rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 12rpx;
}

.modal-desc {
  font-size: 28rpx;
  color: #999;
  margin-bottom: 30rpx;
}

.score-selector {
  display: flex;
  justify-content: center;
  gap: 20rpx;
  margin: 30rpx 0;
}

.select-star {
  font-size: 56rpx;
  opacity: 0.25;
  transition: all 0.2s;
}

.select-star.active {
  opacity: 1;
  transform: scale(1.2);
}

.score-label {
  font-size: 30rpx;
  color: #FF6B8A;
  font-weight: bold;
  margin-bottom: 30rpx;
}

.note-input {
  width: 90%;
  height: 80rpx;
  border: 2rpx solid #EEE;
  border-radius: 16rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  margin: 0 auto 30rpx;
  text-align: left;
}

.modal-actions {
  display: flex;
  justify-content: center;
  gap: 24rpx;
}

.modal-btn {
  padding: 20rpx 50rpx;
  border-radius: 40rpx;
  font-size: 28rpx;
  font-weight: bold;
}

.modal-btn.cancel {
  background: #F5F5F5;
  color: #999;
}

.modal-btn.confirm {
  background: linear-gradient(135deg, #FF9AAB, #FF6B8A);
  color: white;
}

/* 成功提示 */
.success-toast {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(0, 0, 0, 0.75);
  color: white;
  padding: 40rpx 70rpx;
  border-radius: 24rpx;
  text-align: center;
  z-index: 200;
}

.success-icon {
  font-size: 60rpx;
  margin-bottom: 12rpx;
}

.success-text {
  font-size: 30rpx;
}

/* 提醒设置 */
.reminder-card {
  background: white;
  border-radius: 24rpx;
  margin: 0 30rpx 30rpx;
  padding: 30rpx;
  box-shadow: 0 4rpx 16rpx rgba(255, 154, 171, 0.1);
}

.reminder-title {
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 20rpx;
}

.reminder-row {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
}

.reminder-row + .reminder-row {
  border-top: 1rpx solid #F5F5F5;
}

.reminder-label {
  font-size: 28rpx;
  color: #333;
  flex: 1;
}

.reminder-switch {
  margin-right: 20rpx;
}

.reminder-time {
  font-size: 28rpx;
  color: #FF6B8A;
  font-weight: bold;
  padding: 8rpx 16rpx;
  background: rgba(255, 107, 138, 0.08);
  border-radius: 12rpx;
}

.reminder-time.disabled {
  color: #CCC;
  background: #F5F5F5;
}

/* 底部安全区 */
.safe-bottom {
  height: 40rpx;
}

/* ===== 主线故事区域 ===== */
.story-section {
  margin: 20rpx 30rpx;
  background: linear-gradient(135deg, #FFF5F8 0%, #FFE4EC 100%);
  border-radius: 25rpx;
  padding: 25rpx;
  box-shadow: 0 8rpx 25rpx rgba(255, 107, 138, 0.15);
  animation: fadeIn 0.5s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20rpx); }
  to { opacity: 1; transform: translateY(0); }
}

.story-header {
  margin-bottom: 20rpx;
}

.story-chapter-tag {
  display: inline-flex;
  align-items: center;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 20rpx;
  padding: 10rpx 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(255, 107, 138, 0.1);
}

.story-chapter-emoji {
  font-size: 32rpx;
  margin-right: 10rpx;
}

.story-chapter-name {
  font-size: 28rpx;
  color: #E91E63;
  font-weight: bold;
}

/* 敌人信息 */
.story-enemy-info {
  background: rgba(255, 255, 255, 0.6);
  border-radius: 20rpx;
  padding: 20rpx;
}

.enemy-display {
  display: flex;
  align-items: center;
  margin-bottom: 15rpx;
}

.enemy-emoji {
  font-size: 60rpx;
  margin-right: 20rpx;
  animation: enemyBounce 2s ease-in-out infinite;
}

@keyframes enemyBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10rpx); }
}

.enemy-details {
  flex: 1;
}

.enemy-name {
  font-size: 30rpx;
  font-weight: bold;
  color: #E91E63;
  display: block;
  margin-bottom: 10rpx;
}

.enemy-hp-bar {
  display: flex;
  align-items: center;
  gap: 15rpx;
}

.hp-bg {
  flex: 1;
  height: 16rpx;
  background: #FFD6E0;
  border-radius: 8rpx;
  overflow: hidden;
}

.hp-fill {
  height: 100%;
  border-radius: 8rpx;
  transition: width 0.5s ease-out, background 0.3s ease;
}

.hp-text {
  font-size: 22rpx;
  color: #E91E63;
  font-weight: 500;
  min-width: 60rpx;
  text-align: right;
}

/* 任务提示 */
.story-mission {
  text-align: center;
  padding-top: 15rpx;
  border-top: 2rpx dashed #FFB6C1;
}

.mission-text {
  font-size: 26rpx;
  color: #E91E63;
  font-weight: 500;
}

.mission-complete {
  color: #4CAF50;
}
