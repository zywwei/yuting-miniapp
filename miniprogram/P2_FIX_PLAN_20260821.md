# P2 修复计划（Fix Plan · 待确认）

> **日期**：2026-08-21　**基线**：dev 分支 `a4443b9`（P0×8 + P1×20 已全部闭环并提交）
> **范围**：全量审计报告中所有 P2 级条目，去重合并后共 **约 72 项**（含云函数专项中未被前两轮覆盖的遗留 P1，一并按此优先级处理）
> **状态标注**：✅=已被顺带修复　🔧=本期修复　❓=需你先决策　⏸=降级暂缓
> ⚠️ 本计划仅为蓝图；你确认批次范围后我才动手，每批完成后独立提交并附验证记录。

---

## 执行顺序总览

| 批次 | 主题 | 项数 | 风险 | 建议轮次 |
|------|------|------|------|---------|
| A | 主包体验缺陷（notes 族 / parent / like-bar / create） | 13 | 低 | 第 1 轮 |
| B | 云函数安全收口（越权补漏/脱敏/校验） | 14 | 中 | 第 2 轮 |
| C | 数据层健壮性（cloud.js 族 / 时区 / 内存） | 9 | 中 | 第 3 轮 |
| D | 摆摊+画画细节 | 11 | 低 | 第 4 轮 |
| E | 游戏模块（rps/tetris/slots/flight/book） | 13 | 低 | 第 5 轮 |
| F | 学习分包 P2（隔离缺口/同步/竞态/假数量） | 8 | 低 | 第 6 轮 |
| G | 音频与成就口径（speak/beep/audio/achievements） | 8 | 低 | 第 7 轮 |
| H | 习惯分包细节（查重/校验/定时器族） | 9 | 中 | 第 8 轮 |
| I | 仓库与打包卫生 | 5 | 低 | 随任意批捎带 |
| — | 需决策项（见第四节） | 7 | — | 你拍板后插入 |

---

## 批 A：主包体验缺陷（第 1 轮）

| # | 位置 | 问题与修法要点 | 验证 |
|---|------|--------------|------|
| A1 | pages/notes/index.wxml+js 日历 | 选中态存 "2025-6-17" 却按"x月x日"解析→年份当月份。统一存取格式（结构化 year/month/day 或统一 parse） | 切月后高亮保持 |
| A2 | notes/index.js:879 连续删除撤销条互相覆盖 | 新建撤销前 clearTimeout 上一 timer 并对上一条立即执行云端删除 | 快速连删两条均可各自撤销 |
| A3 | notes/add.wxml:203 Math.random() 在表达式内 | 改 CSS keyframes 动画类 | 播放态声波条动画正常 |
| A4 | notes/add.js onUnload 空内容也落草稿 | saveDraft 前复用 hasContent 判空 | 进页即退不产生空草稿、角标不虚增 |
| A5 | notes/add.js 录音资源不释放 | onUnload clearInterval + innerAudioContext.destroy() | 进出页面无报错 |
| A6 | notes/detail.js:171 删除 await 无 try/catch | try/finally 保证 hideLoading | 断网删除不卡 loading |
| A7 | notes/detail.js 语音时长恒 0 秒 | 播放回调读 duration 或持久化时长字段 | 显示真实秒数 |
| A8 | parent 页最近笔记三连 | visibility 过滤（对齐 notes 页逻辑）+ stripHtml 复用 page-helpers + formatDate | 其他成员私密笔记标题不再泄露 |
| A9 | like-bar 无防抖 + toggleLike 入队非幂等 | `_toggling` 锁；入队 action 改为携带目标终态的 setLike | 快速双击仅一次翻转、断网重试不错翻 |
| A10 | comment-section code!==0 幽灵评论残留 | 补 else 分支按 tempId 移除乐观项并 toast 原因 | 审核拒绝时评论消失且有提示 |
| A11 | notes/index _convertCloudUrls 同图只更新末条+索引漂移 | 以 note.id→fileID 映射，回调按 id 重定位 | 两篇同首图笔记缩略图均正常 |
| A12 | create/index.js 今日销售额被当"笔数"展示 | UI 标签改"元今日"或改为 count 计数（取改动小者） | 文案与数值语义一致 |
| A13 | notes/add.js 取消录音 onStop 竞态 | `_canceling` 标志在 onStop 内短路 | 取消后无残留录音 |

---

## 批 B：云函数安全收口（第 2 轮）

| # | 位置 | 问题与修法要点 | 验证 |
|---|------|--------------|------|
| B1 | family joinFamily :175 | removed/disabled 成员凭邀请码重新加入：查重条件改 `status in [active,disabled,removed]` | 被移除成员再加入被拒 |
| B2 | record addRecord :204 客户端自指定 _id 整篇覆盖他人单例 | set 路径补归属校验或移除该特性（倾向后者，调用方核查后定） | grep 全部 add 调用点确认无 _id 传参 |
| B3 | ai-chat getThinkingProgress 凭 taskId 跨家庭读取 | 任务文档记 requesterOpenid/familyId，查询加归属条件 | 伪造 taskId 查询返回空 |
| B4 | ai-chat 限流仅内存且漏计费接口 | speechToText/textToSpeech/testTts 纳入限流；落库计数（openid+日窗口） | 连续调用触发限流返回码 |
| B5 | ai-chat 日志泄露密钥（:1169 整份 systemConfig、:1545 keyPrefix、saveTtsConfig eventKeys） | 删除/脱敏日志字段 | 部署后日志无密钥痕迹 |
| B6 | ai-chat TTS 密钥跨家庭取用（getBaiduKeys/mimoTTS 无 familyId） | 查询加 member.familyId，未配置明确报错 | A 家庭请求不再消耗 B 家庭配额 |
| B7 | ai-chat skillPrompt/extraContext 无长度上限 | 加类型与长度上限（如各 ≤2000），系统指令分段拼接 | 超长入参被拒 |
| B8 | ai-chat getHistory page/pageSize 默认值 | `page\|\|1`、`pageSize\|\|20` 且 clamp | 不传参正常返回 |
| B9 | ai-chat TTL 2h 写 vs 24h 清理不一致；chatStream 不生成标题 | 统一 TTL；stream 完成后同样生成标题 | 任务按时清理、流式会话有标题 |
| B10 | ai-chat buildMessages 历史消息 content 兜底 | 跳过/兜底空 content 历史 | 纯图片历史不炸会话 |
| B11 | interaction targetType 白名单 | 仅允许 COLLECTION_MAP 四值 | 非白名单返回参数错误 |
| B12 | interaction 评论 content≤500 + imageFileId 必须 cloud:// | 校验并截断/拒绝 | 超长与伪 fileID 被拒 |
| B13 | interaction toggleLike 原子化 | `_.push/$.pull` 或事务；客户端 like-bar 配合（A9） | 并发模拟不丢赞 |
| B14 | record listRecords pageSize clamp + 类型校验；memberId 强制取服务端；非 notes 集合通用长度校验 + updates 剔除 likes；catch 区分错误类型 | 四小项一次过 | 参数注入用例全拒 |

---

## 批 C：数据层健壮性（第 3 轮）

| # | 位置 | 问题与修法要点 |
|---|------|--------------|
| C1 | cloud.js migrateLegacyDrawings 空 childId 写完即删 | 迁移前置检查 childId 非空 |
| C2 | cloud.js createSingletonSync.upload 提前写 UpdatedAt | 移到 callUpsertSingleton 成功后 |
| C3 | cloud.js 刷牙更新业务失败(code!==0)不入队 | 与异常路径同样 enqueue |
| C4 | util.js LRU 清理悬空 imagePath | 删文件同时置空对应记录 imagePath |
| C5 | note-manager.js:147 日期边界 UTC/本地混用 | startDate 拼 'T00:00:00' 本地解析 |
| C6 | cloud.js cachedFamilyMembers 缓存无 familyId | key 拼 familyId |
| C7 | cloud.js 墓碑命中重复 remove 且不清墓碑 | remove 成功后 removeTombstone（对齐 fetchDrawings 做法） |
| C8 | backup.js 导出整包双倍内存 | 分批 stringify/提示文件分享导出 |
| C9 | sync-queue 单 key 逼近 1MB | enqueue/saveQueue 包 try/catch + 超限裁剪大项 |

---

## 批 D：摆摊+画画细节（第 4 轮）

D1 stall-manager 金额链路统一 Math.round(x*100)/100（subtotal/total/change）＋删除未用 calculateChange｜D2 history.js deleteSale/switchTab 后 ordersPage/hoursPage 复位｜D3 inventory/price-board 删除商品前统计销售引用并提示｜D4 draw.js 橡皮擦照片模式 destination-out｜D5 draw.js 胶囊避让改 capsule.right｜D6 stats.js 删除 wx.shareAppMessage 死函数｜D7 stats.js month 图表循环改为当月 1 日起｜D8 add-product 上传失败保留原 imagePath｜D9 gallery 日历定位加当前 scrollTop｜D10 撤销栈照片模式降上限至 10｜D11 draw 触摸按 identifier 过滤多点

---

## 批 E：游戏模块细节（第 5 轮）

E1 rps/classic 倒计时挂 this 并 onUnload 清理｜E2 classic 人机局 showResult 即保存战绩｜E3 tournament matchEnd('lose') 接通统计｜E4 game-cloud gameOver 不计入 draws｜E5 tetris initGame 重置 _tetrisCount｜E6 puzzle 未达标拦截发奖｜E7 slots onUnload 按当前 reels 结算退款/派奖｜E8 flight AI 回合链 `_unloaded` 短路｜E9 flight :806 无条件取模规范化｜E10-E13 book toISOString 族换本地日期拼接（add-entry/stats/manager 共 5 处）｜E14 story 透视镜改为回合前预生成展示（25 金币道具生效）

---

## 批 F：学习分包 P2（第 6 轮）

F1 CHILD_KEYS 补 `learnLogs`/`aiCollects`｜F2 learn-data.markAsLearned 补 uploadLearnProgress（对齐同步版本，消除 28 科目进度不同步）｜F3 stats 总数补齐 mathProblems/mathGeometry/mathOlympiad 三键｜F4 cards/poems/numbers/english 翻页 setTimeout 存句柄+onUnload 清理｜F5 math/practice 空答案 toast 拦截｜F6 hub 页 count 从数据层 length 取｜F7 learn-ai-panel 打字机取消令牌防串台｜F8 死组件/死函数清理（quiz-card、filter-bar、stats-chart、formula-card、generateCardsQuiz、practice goDetail）——❓其中孤儿 quiz 三页是否下线需你拍板

---

## 批 G：音频与成就口径（第 7 轮）

G1 speak.js onEnded/onError/换播统一 stop+destroy｜G2 mimo 音色按引擎分 key 存储｜G3 speak 生产 console.log 清理｜G4 beep 包络 min(1,…)+bgm 同型｜G5 audio.toggle 持久化到 settings｜G6 achievements-data 飞行棋 6 成就接 getGameExtraData 或下线（❓二选一）｜G7 "学完全部"阈值改动态取数据长度｜G8 rps_challenge_5 判定统一 level>=N+1

---

## 批 H：习惯分包细节（第 8 轮）

H1 brushing 手动打卡查重改用 fetch 合并结果｜H2 checkin/detail 保存 `_saving` 锁+按钮禁用｜H3 保存前遍历 fields 校验 required｜H4 打卡后重建 habitFields 清 selected｜H5 brushingProgress 改 childStorage（多孩隔离）｜H6 leaveFamily 按 getChildKeys 清业务缓存｜H7 击败特效 prev 值比较+HP 用 ceil｜H8 battle-manager 定时器槽位错开｜H9 补刷按 date+timeOfDay 查重

---

## 批 I：仓库与打包卫生（随批捎带）

I1 删除 cloud-sync-test.js｜I2 删除 generateHappyBGM/testBaiduVoice 死代码｜I3 README 分包结构补 packageLearn｜I4 删除根目录 _listing.txt 与 src/Main.java｜I5 stats.js wx.shareAppMessage 死函数删除（并入 D6）

---

## 第四节：需要你决策的事项（❓）

| # | 事项 | 选项与影响 |
|---|------|-----------|
| D-1 | createFamily 对已有家庭用户拒绝创建，与多家庭模型矛盾 | a) 维持现状 b) 放开多家庭 c) 保持拒绝但文案引导切换家庭 |
| D-2 | 订阅消息提醒（tmplIds 为空） | 需要你在小程序后台申请模板 ID 后提供，否则功能继续搁置 |
| D-3 | 刷牙计时 tick 计数切后台"静默暂停" | a) 有意设计（只算前台刷牙）保持 b) 改 Date.now 校准（锁屏继续计时） |
| D-4 | 早睡卡无时段限制（早上可打早睡） | a) 补时段校验（19:00–次日2:00）b) 保持自由 |
| D-5 | quiz 三页为孤儿页（无入口且引擎刚补齐） | a) 下线页面+引擎瘦身 b) 在科目页加入口启用 |
| D-6 | 隐私 sitemap 当前 allow 全站 | a) 改 disallow 收紧 b) 维持以便搜索曝光 |
| D-7 | project.config.json 被 gitignore（appid 不进版本库） | a) 入库（推荐，团队协作必需）b) 维持 |

## 第五节：验证策略（每批统一执行）

1. 触达 js 全量 node --check；云函数同
2. 可隔离逻辑 mock 冒烟（参照 sync-queue/quiz 先例，断言到字段名）
3. 消费链三问：谁调它/它调谁/WXML 绑定了什么
4. 每批独立 commit（fix(scope): …），附验证记录；推送由你在本机执行
5. 全部批次完成后输出《P2 修复总结》并建议真机回归清单

## 第六节：明确暂缓项（不在本轮）

- saveConfig 显式清除密钥（前端已有空值拦截，误清风险低）
- wx-charts 迁移 type=2d 图表库（工程量大，收益偏视觉）
- 41 个学习详情页模板化重构（架构优化，超出 bug 修复范畴）
- 其余性能优化类条目（打字机 O(n²)、首页三重初始化等）——属优化清单，可在 P2 后另开性能专项
