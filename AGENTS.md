# 全局记忆（Global Memory）

> 本文件是跨会话的全局记忆，每次会话启动时自动生效，无需用户重复提醒。

## 语言规范：所有思考过程必须使用中文

1. **思考过程（thinking / reasoning）必须始终使用简体中文。**
   - 无论用户使用什么语言、无论任务类型（编码、审查、规划、调试、搜索等），内部推理、分析、计划全部用简体中文进行。
   - 不允许出现英文思考段落；技术术语可保留英文原文（如 API、Promise、云函数），但叙述语言必须是中文。

2. **最终回复同样使用简体中文**，使用规范的中文标点（，。！？：""等）。

3. **代码与注释：**
   - 变量名、函数名、类名等代码标识符保持英文，遵循项目现有命名约定；
   - 代码注释、原理解释、修改说明使用中文。

4. **优先级：** 用户当次明确指定的语言要求 > 本规范。除此之外，本规范必须严格遵守。

## 项目背景速记

- 本仓库为微信小程序项目（`miniprogram/`），含云函数（`miniprogram/cloudfunctions/`）。
- 相关技能位于 `skills/`，其中 `chinese-language-preference` 与本记忆配套（更详细的示例见该 SKILL.md）。

## Git 操作纪律：提交与推送必须经用户确认

1. **任何 `git commit` / `git push`（含各种参数变体，如 `git push origin <branch>`、带 `-c` 配置的推送）在执行前必须先向用户报告并得到明确同意。**
   - 未经确认不得提交，即使改动已完成并通过验证。
   - 完成一批修复后，只汇报改动清单与验证结果，等用户指示是否提交。

2. **默认工作模式**：修改代码 → 验证 → 汇报；`commit` 与 `push` 是独立步骤，均需用户逐次确认。

3. **例外**：用户在当次消息中明确说"提交"/"推送"时方可执行，且仅执行当次授权的范围。
